{
	"name": "todo-tracker-cli",
	"version": "1.0.0",
	"description": "Track TODO comments across your codebase with CI enforcement, expiration dates, and git-aware diffing",
	"bin": {
		"todo-tracker": "./dist/cli.js"
	},
	"scripts": {
		"build": "tsc",
		"dev": "tsc --watch",
		"start": "node dist/cli.js"
	},
	"type": "module",
	"dependencies": {
		"chalk": "^5.3.0",
		"commander": "^12.0.0",
		"fast-glob": "^3.3.0",
		"ignore": "^5.3.0"
	},
	"devDependencies": {
		"@types/node": "^20.0.0",
		"typescript": "^5.5.0"
	}
}
