{
  "name": "analyze_income_tax",
  "description": "Analyze income tax rates for a hotel property SPV entity. Returns recommended effective tax rate based on jurisdiction, entity structure, and applicable federal/state/local rates.",
  "input_schema": {
    "type": "object",
    "properties": {
      "location": {
        "type": "string",
        "description": "The property location (city, state/country)"
      },
      "property_level": {
        "type": "string",
        "enum": ["budget", "average", "luxury"],
        "description": "Property tier/positioning"
      },
      "market_region": {
        "type": "string",
        "description": "Market region (e.g., North America, Latin America)"
      },
      "entity_type": {
        "type": "string",
        "description": "SPV entity type (LLC, LP, C-Corp, S-Corp)"
      },
      "purchase_price": {
        "type": "number",
        "description": "Property purchase price for scale context"
      }
    },
    "required": ["location"]
  }
}
