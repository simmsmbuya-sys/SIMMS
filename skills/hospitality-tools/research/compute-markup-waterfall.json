{
  "name": "compute_markup_waterfall",
  "description": "Compute the full cost-plus markup waterfall for a centralized service: vendor cost to fee charged to gross profit to effective margin. Includes industry markup benchmarks by service type.",
  "input_schema": {
    "type": "object",
    "properties": {
      "vendorCost": {
        "type": "number",
        "description": "Annual vendor cost in dollars"
      },
      "markupPct": {
        "type": "number",
        "description": "Cost-plus markup as decimal (0.20 = 20%)"
      },
      "serviceType": {
        "type": "string",
        "description": "Optional service type for industry benchmark lookup: marketing, it, accounting, revenue_management, procurement, hr, design, general_management"
      }
    },
    "required": ["vendorCost", "markupPct"]
  }
}
