{
  "name": "verify_report_completeness",
  "description": "Verify that all financial reports and exports are complete and consistent. Check that all export formats (PDF, Excel, CSV, PowerPoint, Chart PNG, Table PNG) generate without errors and contain matching values.",
  "parameters": {
    "type": "object",
    "properties": {
      "reportType": { "type": "string", "enum": ["property_income_statement", "property_cash_flow", "property_balance_sheet", "company_income_statement", "company_cash_flow", "portfolio_consolidated", "dashboard_kpis"], "description": "Type of financial report to verify" },
      "propertyId": { "type": "integer", "description": "Property ID (for property-specific reports)" },
      "exportFormats": { "type": "array", "items": { "type": "string", "enum": ["pdf", "xlsx", "csv", "pptx", "chart_png", "table_png"] }, "description": "Export formats to verify" },
      "checkOnScreenMatch": { "type": "boolean", "description": "Whether exported values must match on-screen display", "default": true },
      "projectionYears": { "type": "integer", "description": "Number of projection years to verify coverage for" }
    },
    "required": ["reportType"]
  }
}
