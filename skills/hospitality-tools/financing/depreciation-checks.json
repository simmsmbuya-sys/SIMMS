{
  "name": "verify_depreciation",
  "description": "Verify straight-line depreciation per ASC 360. Depreciable basis = (purchasePrice × (1 - landValuePercent)) + buildingImprovements. Annual depreciation = depreciableBasis / 27.5. Monthly = annual / 12.",
  "parameters": {
    "type": "object",
    "properties": {
      "propertyId": { "type": "integer", "description": "Property ID to verify depreciation for" },
      "purchasePrice": { "type": "number", "description": "Property purchase price" },
      "landValuePercent": { "type": "number", "description": "Land value as percentage (e.g., 0.25 for 25%)" },
      "buildingImprovements": { "type": "number", "description": "Building improvement costs" },
      "reportedAnnualDepreciation": { "type": "number", "description": "Annual depreciation amount reported by the application" },
      "reportedMonthlyDepreciation": { "type": "number", "description": "Monthly depreciation amount reported by the application" }
    },
    "required": ["purchasePrice", "landValuePercent", "buildingImprovements"]
  }
}
