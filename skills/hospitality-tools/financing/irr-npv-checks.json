{
  "name": "verify_irr_npv",
  "description": "Verify IRR calculation by confirming NPV≈0 at the computed IRR. Test both property-level and portfolio-level IRR. Verify FCF series used for IRR calculation matches the cash flow statement.",
  "parameters": {
    "type": "object",
    "properties": {
      "propertyId": { "type": "integer", "description": "Property ID to verify IRR for" },
      "computedIrr": { "type": "number", "description": "The IRR value computed by the application (as decimal, e.g., 0.12 for 12%)" },
      "fcfSeries": { "type": "array", "items": { "type": "number" }, "description": "Array of annual Free Cash Flow values used in IRR calculation" },
      "initialInvestment": { "type": "number", "description": "Initial equity investment (negative value)" },
      "terminalValue": { "type": "number", "description": "Terminal/exit value included in final year FCF" },
      "tolerance": { "type": "number", "description": "Acceptable NPV tolerance (default 0.01)", "default": 0.01 }
    },
    "required": ["computedIrr", "fcfSeries", "initialInvestment"]
  }
}
