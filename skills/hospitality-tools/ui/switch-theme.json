{
  "name": "switch_theme",
  "description": "Switch the active theme for the current user session. Applies theme tokens to CSS custom properties.",
  "parameters": {
    "type": "object",
    "properties": {
      "theme_name": {
        "type": "string",
        "description": "Name of theme to activate (built-in or user-created)"
      }
    },
    "required": ["theme_name"]
  }
}
