{
  "name": "variance_report",
  "description": "Generate variance analysis comparing two scenarios or baseline vs projected across all financial metrics.",
  "parameters": {
    "type": "object",
    "properties": {
      "scenario_a": {
        "type": "string",
        "description": "Name or ID of first scenario (baseline)"
      },
      "scenario_b": {
        "type": "string",
        "description": "Name or ID of second scenario (comparison)"
      },
      "property_id": {
        "type": "number",
        "description": "Property ID (optional, defaults to portfolio-level)"
      },
      "threshold_pct": {
        "type": "number",
        "description": "Minimum variance percentage to flag as material (default 1%)"
      }
    },
    "required": ["scenario_a", "scenario_b"]
  }
}
