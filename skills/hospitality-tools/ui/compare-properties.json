{
  "name": "compare_properties",
  "description": "Generate side-by-side comparison data for 2-4 properties across financial KPIs.",
  "parameters": {
    "type": "object",
    "properties": {
      "property_ids": {
        "type": "array",
        "items": { "type": "number" },
        "minItems": 2,
        "maxItems": 4,
        "description": "IDs of properties to compare"
      },
      "metrics": {
        "type": "array",
        "items": { "type": "string" },
        "description": "KPI metrics to compare (e.g., revenue, noi, irr, occupancy, adr, dscr)"
      },
      "year": {
        "type": "number",
        "description": "Specific projection year to compare (optional, defaults to all years)"
      }
    },
    "required": ["property_ids"]
  }
}
